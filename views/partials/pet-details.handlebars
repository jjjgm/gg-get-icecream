<div class="container-fluid mt-4">
    <div class="row">
        <div class="col-4">
            <img id="match-pic" src={{petImage}} alt={{petImage}}>
        </div>
        <div class="col-8">
            <h1>{{pet.name}}</h1>
            <h3>age: {{pet.age}}</h3>
            <h3>🌐 {{pet.location}}</h3>
        </div>
        
        <div class="mt-4">
            <h6>Bio</h6>
                <p>
                    {{bio}}
                </p>
        </div>